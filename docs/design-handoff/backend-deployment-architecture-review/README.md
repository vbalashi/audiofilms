# Backend Deployment Architecture Review Package

This folder contains the prompt and context for an external senior architecture
review of the AudioFilms backend deployment plan.

Start with:

```text
REVIEW_BRIEF_RU.md
```

The zip package generated for review should include source files and docs, but
exclude local dependencies, build output, caches, git internals, and secret env
files.
